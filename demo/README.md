# Generated demo fixtures

Run:

```bash
venv/bin/rewind demo --output /tmp/rewind-demo
```

Fixtures are generated into a disposable directory so their signed identities, Git object IDs, and local recorder state cannot be mistaken for production or committed project state. See `DEMO.md` for the complete recording path.

